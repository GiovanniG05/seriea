import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calciolive.app',
  appName: 'CalcioLive',
  webDir: 'dist/ng-starter/browser'
};

export default config;